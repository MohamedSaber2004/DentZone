 "/api/Products/search-product-category": {
      "get": {
        "tags": [
          "Products"
        ],
        "parameters": [
          {
            "name": "catId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "inventoryId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "search",
            "in": "query",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    } replace that endpoint with that   byProvider: (inventoryId: string, search?: string) =>
    `${base}/Products/GetProducts-byProvider?inventoryId=${inventoryId}${search ? `&search=${encodeURIComponent(search)}` : ''}`,
